# Security Policy

Report vulnerabilities privately to the repository maintainers. Do not open public issues containing credentials or customer data. Rotate all example secrets, restrict the admin endpoint, terminate TLS at a trusted proxy, keep dependencies patched, and never commit `.env`, private keys, dumps, uploads or logs.
